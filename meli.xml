<context>
	<input pattern="* (hola) *">
		<output>
			<item>Hola soy tu asistente para buscar productos en Mercado Libre, para continuar solo ingresa tu busqueda y un precio que estas dispuesto a pagar para buscar coincidencias, el precio es opcional. Ej: Me gustaria comprar una notebook por 7000</item>
		</output>
		<context id="buscador">
			<input pattern="* (quiero|gustaria|deseo|necesito)* [$Busqueda::Text] [(por|a|entre los|)*] [$Precio::Number]">
				<output value="$Busqueda a $Precio" if="full($Busqueda) and full($Precio)"/>
				<output value="$Busqueda" if="full($Busqueda)"/>
				<output value="$Precio" if="full($Precio)"/>
				<output value="Debe ingresar una busqueda" if="empty($Busqueda) and empty($Precio)"/>
			</input>
		</context>

	</input>	
</context>